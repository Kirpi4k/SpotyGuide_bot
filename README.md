---

# SpotyGuide 🎵

**SpotyGuide** — это Telegram-бот для поиска, просмотра и добавления музыки из Spotify. Бот интегрирован с Spotify через OAuth 2.0 и предоставляет удобный интерфейс для работы с треками и плейлистами прямо в Telegram.

---

## 📌 Основные возможности

* 🔍 Поиск треков по названию или исполнителю
* 🎧 Просмотр информации о треке: название, исполнитель, ссылка на Spotify
* ➕ Добавление треков в пользовательские плейлисты
* 🔐 Полная интеграция с Spotify через OAuth 2.0
* 🌐 Поддержка локального сервера через ngrok для работы OAuth

---

## 🛠 Требования

* Python 3.10 или выше
* Telegram-бот (токен)
* Аккаунт Spotify для OAuth авторизации
* PostgreSQL (для хранения данных пользователей и плейлистов)
* [ngrok](https://ngrok.com/) для проброса локального сервера в интернет (только для локальной работы)

---

## ⚙️ Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/ваш_репозиторий/SpotyGuide.git
cd SpotyGuide
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Настройте переменные окружения (`.env`):

```env
TELEGRAM_TOKEN=ваш_токен_бота
SPOTIFY_CLIENT_ID=ваш_client_id
SPOTIFY_CLIENT_SECRET=ваш_client_secret
SPOTIFY_REDIRECT_URI=http://localhost:8080/callback
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
```

> ⚠️ Для локальной разработки используется **ngrok** для проброса локального сервера в интернет.

---

## 🚀 Настройка ngrok для локального сервера

1. Скачайте и установите ngrok:

* [Официальный сайт](https://ngrok.com/download)
* Распакуйте архив и добавьте в PATH (или просто запустите из папки с ngrok)

2. Запустите локальный OAuth-сервер (например, на порту 8080):

```bash
python auth_server.py
```

3. Проброс локального сервера через ngrok:

```bash
ngrok http 8080
```

4. Ngrok выдаст публичный URL вида `https://abcdef1234.ngrok.io`.

* **Важно:** замените переменную `SPOTIFY_REDIRECT_URI` в `.env` на этот URL + `/callback`, например:

```
SPOTIFY_REDIRECT_URI=https://abcdef1234.ngrok.io/callback
```

5. Перезапустите бота — теперь Spotify сможет корректно отправлять callback-запросы.

> 🔹 Важно: при каждом новом запуске ngrok URL меняется, поэтому его нужно обновлять в `.env`.

---

## 🚀 Запуск бота

```bash
python main.py
```

Бот будет доступен в Telegram и готов к работе.

---

## 📋 Основные команды

| Команда                         | Описание                                |
| ------------------------------- | --------------------------------------- |
| `/start`                        | Запуск бота и приветствие               |
| `/menu`                         | Просмотр Inline - menu                  |

---

## 📂 Структура проекта

```
SpotyGuide/
│
├─ bot.py            # основной скрипт бота
├─ db.py             # работа с базой данных
├─ config.py         # зависимости
└─ requirements.txt  # зависимости
```

---
